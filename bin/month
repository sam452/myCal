#!/usr/bin/env ruby
if ARGV.length > 1
  month = ARGV[0].to_i
  year = ARGV[1].to_i
  cal = Month.new(month, year)
  puts cal.printout
  else
    year = ARGV[0].to_i
    cal = Calendar.new(year)
    puts cal.printout
end


